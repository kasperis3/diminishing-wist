

export interface PlayerCard {
    bets: number[] // length of rounds
    gets: number[]
    scores: number[]
    position: number // aka id
    name?: string
}

export interface Round {
    // suit
    // nextSuit
    // hands
    // playerStart
    // nextHand
}

